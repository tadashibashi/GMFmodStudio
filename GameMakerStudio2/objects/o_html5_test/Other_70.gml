/// @description Insert description here
// You can write your code in this editor
var map = async_load;

if (async_load[?"id"] == "mysocialasyncdata")
{
	show_message(string(async_load[?"val"]));
	show_message(string(async_load[?"name"]));
}
